[build]
  # Lệnh để build dự án (Thay đổi tùy theo framework bạn dùng)
  command = "npm run build"
  
  # Thư mục chứa sản phẩm cuối cùng để Netlify phục vụ
  publish = "dist"

# Cấu hình chuyển hướng cho các ứng dụng Single Page (SPA)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200